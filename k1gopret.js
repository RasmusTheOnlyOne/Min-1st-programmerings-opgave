//Hello world 
document.addEventListener('DOMContentLoaded'), (event) => {
    document.getElementById('form').addEventListener('submit', (event) => {
        event.preventDefault(); //Dette var et tip fra Særen video, så siden ikke relaoder ved submit
    
    fetch
    })
    console.log("Login");
}